<!--
  IN PROGRESS — the notes for the NEXT release. Add to this file as work lands on main, so the
  release notes are already written when 0.1.03 is cut rather than being reconstructed from
  `git log` afterwards.

  BEFORE DISPATCHING THE RELEASE WORKFLOW:

    1. DELETE the "Coming in 0.1.03" banner immediately below. release.yml prepends this whole
       file verbatim as the Highlights section of the published GitHub Release, so leaving it in
       would tell people the release they are reading has not happened yet.
    2. Re-check the "Upgrading?" line. It currently says no repatch is needed, which is true of
       everything in here so far — but it stops being true the moment anything bumps
       ApplicationPatcher.Version, and a wrong answer there leaves people with games that will
       not launch.
    3. Check the version in the heading matches what you type into the workflow.
    4. Update README.md: replace the "What's new in 0.1.02" section with this release's
       highlights, and DELETE the "🚧 Coming in 0.1.03" block underneath it — otherwise the front
       page still tells people the release is unreleased after it has shipped.
    5. Src/Directory.Build.props already says 0.1.03; the workflow overrides it from the dispatch
       input, so there is nothing to bump for THIS release — bump it to the next version after
       shipping.

  See Docs/RELEASING.md for the rest of the process.
-->

> ### 🚧 Coming in 0.1.03 — not released yet
>
> This is what is queued for the next release. It has landed on `main` but is **not in any
> published build**, so none of it is on the [Releases](https://github.com/Bubbleshum/WPR/releases)
> page yet. The list below grows as more work lands, until 0.1.03 ships.

---

**Your phone buzzes, and portrait games steer by tilt.** Vibration was never implemented at all,
and a whole class of tilt-steered games sat perfectly still no matter how the phone was held.

| | |
| --- | --- |
| 📳 | **Games can vibrate your phone on Android.** Collisions, wrong answers, menu taps — all of it was silent before |
| 🔕 | **A single vibration on/off switch** in Settings, covering every game and any connected controller |
| 🧭 | **Tilt works in games that never responded to it** — Doodle Jump among them, on Windows as well as Android |

> **Upgrading?** Nothing to do — no repatch or reinstall. Installed games pick this up the next
> time you launch them.

---

### Games can vibrate your phone

Windows Phone games buzz constantly — when you crash a car, get an answer wrong, or just tap a
menu item. On WPR that has never once happened. The part of the runtime a game calls to vibrate
the phone existed, but did nothing at all: it accepted the request and returned. No error, no
warning, nothing in any log. Games behaved as though it had worked.

It now drives the real motor on Android, including **vibration strength** on phones whose hardware
supports it, rather than everything being full-force.

Windows is unchanged — a desktop PC has no vibration motor.

### Turn vibration off, everywhere, in one place

**Settings → VIBRATION** on Android. One switch, and it applies to **every game** — there is
nothing to set per game.

It also covers **controller rumble**, so switching vibration off really does mean off, rather than
silencing the phone and leaving a connected gamepad shaking.

Flicking it on gives a short confirmation buzz, the same way the phone's own haptics settings do —
so you can tell "switched on" apart from "switched on but this device has no motor". If your
device has no motor, the page says so instead of leaving you guessing.

A game that is already running keeps whatever setting it started with; the change applies from the
next time you launch it.

### Tilt works in games that ignored it completely

Some games never responded to tilt at all — on Windows as well as Android.

Windows Phone reports tilt relative to the phone itself, not to whatever way up the game is drawn,
so a game that supports more than one screen orientation rotates the readings itself to match. To
do that it asks the runtime which way the screen is currently oriented — and WPR never answered.
Games written to handle that carefully would rather ignore a reading than act on one they cannot
interpret, so they threw every single one away and sat perfectly still.

**Doodle Jump** is the clearest example: the whole game is steered by tilting, and the character
simply would not move. It now plays properly, in portrait, on both Windows and Android.

Any tilt-steered game previously written off as unplayable is worth another go.

The Android download is also about **10 MB smaller** — reading the phone's motion sensor no longer
needs the third-party library it used to, so that library and everything it dragged in are gone.
